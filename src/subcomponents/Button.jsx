const Button = ({text}) => {

    return (
        <>
            <button
                className="text-lg
                 border-2 border-black w-30">{text}</button>
        </>
    );
};

export default Button;